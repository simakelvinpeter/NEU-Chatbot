# Install Material-UI

Please run this command in your frontend folder terminal:

```bash
npm install
```

This will install the Material-UI packages that were added to package.json:
- @mui/material
- @emotion/react
- @emotion/styled

After installation, the frontend server will automatically reload with the new changes.
